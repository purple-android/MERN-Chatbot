# Used by `npm run build` (the version you host online, e.g. on Render).
# Points the hosted frontend at your local backend exposed via ngrok.
# (You can also override this in the host's dashboard env vars — that takes precedence.)
REACT_APP_API_URL=https://rocking-seventh-sadden.ngrok-free.dev
